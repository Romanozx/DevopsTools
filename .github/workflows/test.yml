main: Pipeline teste

on: 
    push:
        branches: 
            - dev

jobs:
    validar-push-dev:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v2
            - name: Validate index.html exists
              run: test -f index.html
